class AuthService {
  async init() {
    // Authentication system has been removed.
    // The app works without login; all LLM providers are user-configured.
  }
}

export const authService = new AuthService();
