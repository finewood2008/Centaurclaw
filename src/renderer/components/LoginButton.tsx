import React from 'react';

// Login/account functionality has been removed.
// All LLM providers are user-configured via the Settings page.
const LoginButton: React.FC = () => null;

export default LoginButton;
